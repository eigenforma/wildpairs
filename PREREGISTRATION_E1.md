# PREREGISTRATION — E1: The Institution Flagged It

> **STATUS: DRAFT.** This document binds nothing until frozen by the tagged commit `prereg-e1`.
> **Declaration at draft time (2026-08-05): no encoder has been loaded against any errata pair. No cosine score over any errata text exists anywhere in this program.** The only computations performed before this freeze are the encoder-blind snapshot verifications listed in §6, all lexical (counts, token-Jaccard, permutation tests), recomputable via `scripts/verify_snapshot.py`.

## 1. Population

- Source: `corpus/e1_errata/errata.json`, frozen snapshot (SHA-256 in `corpus/MANIFEST.json`). Analyses never touch the live URL.
- Primary population: entries with `errata_status_code == "Verified"`, `errata_type_code ∈ {"Technical","Editorial"}`, and both `orig_text` and `correct_text` non-empty after whitespace normalization.
- Sensitivity stratum: `"Held for Document Update"`, same filters, analyzed identically, reported separately, never pooled with primary.
- Exclusion — code-primary pairs: a pair is code-primary if either text has alphabetic-character ratio < 0.70 over non-whitespace characters, or ≥ 50% of its lines match the ABNF/code indicator set (`::=`, `=/`, lines that are purely punctuation/hex/bit-diagram characters). Code-primary pairs form a separate register stratum, reported if n ≥ 200, else only counted. The exact predicate is `harness/lib/filters.py::is_code_primary` at the freeze tag; its yield on the frozen snapshot is reported before any encoder runs.

## 2. Instruments, all fixed before the freeze

- Tokenizer for all lexical statistics: lowercase; tokens are maximal `[a-z0-9]+` runs. Token-Jaccard is set-based.
- Granularities per pair: (g1) changed-sentence — minimal diff-localized sentence(s); (g2) quoted-passage — the `orig_text`/`correct_text` fields as filed; (g3) section-context — the erratum's quoted text re-located in the frozen RFC full text and expanded to its section, best-effort, with match rate disclosed; g3 failures drop from g3 only.
- Edit classes over Verified-Technical (machine-labeled, cascade-arbitrated, human-audited on 300 stratified pairs): RFC-2119 keyword-strength change (case-sensitive multiset over MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT, RECOMMENDED, NOT RECOMMENDED, MAY, OPTIONAL); quantity/number change (`\d+(\.\d+)?` multiset); polarity/negation; identifier/constant; scope/other.
- Encoders: the nine pinned polaritycheck configurations, checkpoint hashes carried over from the Paper A artifact, plus operating points cosine 0.30 / 0.40 / 0.60 / 0.80 / 0.85 / 0.95.

## 3. Hypotheses and numeric bars (pass/fail is per-bar, no post-hoc softening)

- **H1 (gates approve corrections).** At thresholds ≥ 0.80, ≥ 90% of Verified-Technical pairs score above threshold at g2; median Technical cosine ≥ 0.95.
- **H2 (little decision content even unconfounded).** Marginal Technical-vs-Editorial AUROC ∈ [0.45, 0.65] for all nine configurations at g2, AND overlap-decile stratification moves each configuration's estimate by < 0.05. If the second clause fails, the corpus is NOT certified naturally matched and all decision claims are reported stratified-only.
- **H3 (worst-classes replication).** Modal-keyword and quantity classes have the two highest median cosines among Technical edit classes, each ≥ 0.97, for ≥ 7 of 9 configurations.
- **H4 (calibrations do not transfer).** Each repair (per-encoder optimal threshold; logistic on cosine + token-Jaccard; NLI cross-encoder) fitted on errata drops ≥ 0.10 balanced accuracy or AUROC when frozen-evaluated on the bis corpus.
- **H5 (dilution in the wild).** Mean decision AUROC (over configurations) decays monotonically g1 → g2 → g3 with total drop ≥ 0.10.

## 4. Analysis plan

Marginal + overlap-decile-stratified AUROC per (configuration, granularity), cluster bootstrap by RFC document number (10,000 resamples, seed 20260805); balanced accuracy and firing rate at the six operating points; per-class median cosine and class AUROC; Cliff's δ + permutation test (mean-difference statistic, 20,000 permutations, seed 20260805) for the overlap-class coupling; Spearman cosine~Jaccard within class; κ (Cohen) on both annotation arms with adjudicated label-noise rate reported as a ceiling on every decision-axis claim.

## 5. Bis corpus (held-out; mined AFTER errata analysis is frozen, from a disjoint pipeline)

All obsoletes/obsoleted-by families from the frozen `rfc-index.xml`; alignment through intermediate revisions only (never skipping steps); pre-registered artifact taxonomy excluded before labeling: case normalization (`MUST not`→`MUST NOT`), prose→keyword normalization (`are permitted to`→`MAY`), subject rewording without strength change, alignment errors. Gate: ≥ 100 human-verified genuine strength transitions or the transfer arm reports "insufficient natural positives" as a finding — the gate cannot be lowered after mining begins. 200-pair two-annotator alignment audit.

## 6. Computed before this draft (disclosed, encoder-blind, all lexical)

All values recomputed by the named scripts from the frozen snapshot (sha256 `7e086e85fd5f…`); two-source checks against the 2026-08-05 independent workflow-verifier parse matched exactly on every count. No embedding, no cosine, no encoder has touched any errata text.

- **Population** (`scripts/verify_snapshot.py`): 7,991 entries; Verified 3,710 / Held 2,412 / Rejected 1,151 / Reported 718; Verified with both texts: **1,895 Technical, 1,683 Editorial**.
- **Edit-class counts** (same script): RFC-2119 keyword-multiset changes **122**; number-multiset changes **782** (Verified-Technical).
- **Overlap coupling** (same script; informs H2's stratification clause): token-Jaccard median **0.8889 (T)** vs **0.9130 (E)**; means 0.7959/0.8265; **Cliff's δ = −0.1005** (negligible by convention, benign direction — Technical slightly MORE reworded); permutation p = 1e-4 (mean-diff, 20,000 perms, seed 20260805). The coupling is small but statistically detectable: the corpus is *near*-matched by nature, so decile-stratified reporting stays mandatory and H2's <0.05 movement clause is the certification test.
- **Granularity build** (`harness/e1/build_granularities.py`): 5,701 pairs emitted (91 pure-whitespace corrections excluded); primary Verified-prose corpus **1,425 Technical vs 1,402 Editorial**; code-primary stratum 1,005 pairs total (445 Verified-Technical — reported separately per §1); Held-prose sensitivity stratum 673 T / 1,196 E. Changed-sentence localization (Verified prose): **1 slot: 2,246 (79.4%)**, 2 slots: 360, ≥3: 221.
- **g3 relocation** (`harness/e1/locate_sections.py`, best-effort arm, run on the frozen coldstore RFC corpus): overall match rate **0.8292** (4,727/5,701); Verified-prose: Technical **86.3%**, Editorial **89.1%** — inside the pre-registered 70–90% expectation, predominantly full-quote matches. g3 failures drop from g3 analyses only.

These numbers informed H2's stratification clause and H3's class choices. Nothing else was computed.

## 7. Freeze protocol

Freezing = one commit that (a) finalizes this file with §6's actual numbers cited from `results/verification/e1_snapshot_verification.json`, (b) pins `harness/lib/filters.py`, (c) is tagged `prereg-e1`. Every subsequent change to this file is an amendment section below the tag line, never an edit above it.

---

## Amendments (append-only, below the `prereg-e1` tag line)

> **All PROPOSED 2026-08-08** from the five-lens adversarial review (`docs/reviews/adversarial_review_2026-08-08.md`); the operator's commit is the ratification. A1 is the decision brief's D2, recommended 2026-08-06 and not yet filed. Nothing above the divider changes; every failed bar stays exactly as failed.

- **A0 — tag discharge.** The banner's conditional ("binds nothing until frozen by tagged commit `prereg-e1`") was discharged when the tag landed at `78543a2` (2026-08-05). Everything above this divider is the frozen text; external copies (outreach attachments) should carry this note so "STATUS: DRAFT" is not misread as unfrozen.
- **A1 — post-hoc analyses already run, registered as post-hoc (D2).** (i) The three-regime coupling-orientation analysis (the sign-flip table: authored ≈ inverted / errata ≈ near-zero / bis ≈ aligned, machine-labeled with the audit's measured correction). (ii) The per-class bis Jaccard/cosine breakdown. Both were computed after encoder results were seen; both are labeled post-hoc wherever they appear.
- **A2 — bis↔errata incorporation-leakage probe (registered with result).** Attack: verified errata are incorporated into bis revisions, so H4's eval set could contain the very transitions the calibrations were fit on. Measured on the frozen corpora (2026-08-08, both-side token-set Jaccard vs same-document errata): **16 of 13,780 bis pairs (0.12%) at J ≥ 0.9; 3 of 2,019 strength transitions (0.15%); 46 (0.33%) at the loosest J ≥ 0.5** — bounded far below any level that could move the transfer estimates (specimen: `bis-RFC3028-RFC5228-711` ≡ erratum `eid1493` at J = 1.0). Sensitivity: H4 recomputed excluding matched pairs; probe script preserved with the review record.
- **A3 — cluster-honest permutation.** §4's permutation test permuted across errata pairs; errata cluster by RFC document (batch filings, shared reporters), violating exchangeability — the anti-conservative direction Paper A §3 corrected in itself. Redo: permutations at document level, reported beside the original p; Cliff's δ (an effect size, unaffected) remains the operative quantity, with |δ| < 0.10 as the equivalence criterion and significance-at-this-n declared irrelevant.
- **A4 — lexical-baseline panel and size controls (post-hoc, from frozen scores + lexical stats).** (i) Jaccard-alone, normalized-edit-distance, and TF-IDF-cosine AUROC beside every encoder AUROC at g1/g2 (review measurement: Jaccard alone ≈ 0.55 full / ≈ 0.57 prose-primary — approximately the naive encoder result; "the embedding does not outperform counting words" requires this panel to be sayable). (ii) Cosine-given-Jaccard partial analysis. (iii) Passage-length deciles as a second stratification axis. (iv) The size-matched one-token contrast — Technical `MUST→SHOULD` vs Editorial `teh→the` — as a named analysis. (v) r(Jaccard, class) at g3: the non-arithmetic counterpart of E2-H5, registered here.
- **A5 — the 300-pair label-noise arm (open commitment, protocol upgraded before execution).** Annotators judge the **construct**, blind to the IETF type: "does `correct_text` change what `orig_text` asserts, within the compared span?" Deliverables: construct-vs-institution confusion matrix (a named result); adjudicated noise rate ρ̂ per class with CI; noise-corrected AUROC A_corr = (A_obs − ρ̂)/(1 − 2ρ̂) printed beside every decision-axis figure as the ceiling; promotion trigger — if construct disagreement > 15% in either class, the adjudicated-construct subsample becomes the primary decision corpus. Staffing per the bis-audit precedent (two firewalled annotator contexts + operator adjudication, machine labels sealed); the roles policy keeps all outreach reviewers ineligible until annotation closes. **[EXECUTED 2026-08-10/11: packet built (seed 20260810), both seats flown blind, κ(A,B) = 0.6813, operator sitting closed 2026-08-11. Technical precision 95.2% (kw2119 47/47, number 49/49); Editorial construct leak 66.0%; ρ_T = 0.048, ρ_E = 0.660; institution-label AUROC ceiling 0.646; **promotion trigger FIRED** — adjudicated subsample promoted to primary; promoted-primary AUROC 0.71–0.80 with the Jaccard baseline at 0.772 covering all nine (production path below it). `docs/findings_errata_audit_2026-08-11.md`.]**
- **A6 — gate-term reporting (post-hoc, from frozen results).** Per-threshold 2×2 (class × gate action) including the faithful/Editorial false-block direction; |pass(Technical) − pass(Editorial)| at every operating point; and the continuity number at cosine 0.60 — the audited guard's operating point (Paper A §6) — reported prominently: the 0/56 guard, now against ~1,425 institution-verified prose corrections.
- **A7 — operating-point provenance wording.** Everywhere "six shipped operating points" appears, the paper-facing form is "the five shipped operating points of Paper A §5 plus the audited guard's cosine-0.60 equivalent (§6)" — Paper A says *five*, and the same reviewers will diff the count.
- **A8 — audit close, artifacts and the computed correction (campaign-auditor lens).** The 200-pair bis audit closed 2026-08-07: precision 84.8% (84/99), negative-class leak 37% (37/100), κ(A,B) = 0.662 (`results/verification/bis_audit_final.json`). **Time-sensitive: annotator B's verdict vector was written to `/tmp` on a fleet node and annotator A's is uncommitted — retrieve both and commit under `results/verification/annotator_{A,B}_judgments.json` before a reboot destroys the audit's recompute path.** findings_h4's "attenuated under true labels… this measured correction" names a number that exists nowhere; the review computed the admissible bounds from the 35 operator verdicts + frozen aggregates: true-label median-Jaccard gap between **+0.080 and +0.259** (exchangeability estimate +0.159), **aligned in 100% of admissible reconstructions** — the sign-flip's bis leg survives its own audit adversarially. Close by committing the vectors, adding `scripts/bis_true_label_coupling.py` for the exact corrected medians, and regenerating `results/verification/e1_bis_stats.json` to carry the per-class medians (0.6667/0.8696) it is already cited for. **[EXECUTED 2026-08-10: both vectors located in a prior session's scratchpad on Wu, rescued to `results/verification/annotator_{A,B}_judgments.json`; exact true-label medians 0.6842/0.8988, gap +0.2146 (machine gap +0.2162 — attenuation negligible), inside the review bounds; every audit aggregate reproduced as a checksum. Frozen: `results/verification/bis_true_label_coupling.json`.]**
- **A9 — instrument deviation, disclosed.** §2 registered five edit classes "machine-labeled, cascade-arbitrated"; H3 was scored on a **three-class partition** (kw2119 / number / other — `results/e1_analysis.json`; the E1-P3 cascade never ran, RUNBOOK R2 never triggered, no 120B ever labeled an errata pair). H3's FAIL verdict is robust to the partition (kw2119 g2 median cosine 0.9183–0.9832, below the 0.97 bar and never top-2 under any subdivision of "other"), but a pre-registered instrument was replaced by a coarser one without disclosure — this amendment is that disclosure. Resolution required before drafting: either run the cascade (R2) and report the refined class table as labeled post-hoc, or formally retire the three cascade-only classes here.
- **A9 CLOSED 2026-08-12 by execution.** The cascade ran (bulk 20B + 556 blind 120B
  arbitrations). H3's registered verdict is unchanged and now partition-robust by measurement:
  **0/9 under both the three-class regex partition it was scored on and the five-class semantic
  partition it was registered for.** Refinement reported as labeled post-hoc:
  `docs/findings_cascade_2026-08-12.md`. Two facts the refinement adds — the regex partition is
  only 48%/76% precise against semantic labels, and the per-class blindness ordering *inverts*
  relative to Paper A §6 (modal keywords most visible, quantity least).
