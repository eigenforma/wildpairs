# Adversarial review — the Paper B campaign (2026-08-08)

**Provenance.** Five parallel review lenses, orchestrator-synthesized, run at the operator's request ("I yearn for an adversarial review of that paper and a sharpening pass"). Four lenses attacked the 2026-08-05 design record (`docs/experiments.md`): **statistics/methodology** (16 findings), **framing + Paper-A cross-consistency** (14 findings, 20-row consistency table, IETF definitions fetched live), **data reality** (fresh downloads of every claimed source, full-corpus recomputation), **novelty/scoop hunt** (26 web searches, 20 fetches, 47 NevIR citations swept). A fifth lens audits the live campaign artifacts (preregistrations, findings docs, discipline) — §9. The orchestrator independently verified the sharpest claims (leakage probe executed; tag contents inspected; Paper A figures quote-checked).

**Verdict.** Neither headline is scooped: no prior work uses RFC errata with any text model (McQuistin TMA'23 confirmed sole and correctly characterized, erratum-5595 verbatim — and the boundary specimen is itself a missing "not"); no NevIR descendant adds a length axis; nothing anywhere measures a flip-vs-faithful decision decay or a critical length against shipped operating points. The evidence base holds: every load-bearing count reproduced on a fresh 2026-08-08 download, and the frozen `errata.json` is **byte-identical to today's live file** (sha256 `7e086e85fd5f…` both sides — zero drift in three days). The discipline held where it mattered: encoder-blind ordering, bars frozen before measurement, failed bars reported as failed. The review's teeth landed on five surfaces: a time-critical externality (§1), unfrozen-E2 semantics (§2 — fixed today, no bar values moved), citation corrections (§5), framing debts the paper must pay when drafting starts (§5), and discipline hygiene (§3, §8).

---

## 1. Time-critical (this week)

- **PMC slice: freeze before 2026-08-24.** Verified live: the license-partitioned bulk path exists only under `s3://pmc-oa-opendata/deprecated/oa_comm/txt/all/` (moved 2026-04-13; top-level `oa_comm/txt/` already empty), and NCBI's announced deletion is "on or after 2026-08-24" — 16 days out. E2's plan deferred PMC to the prereg-e2 pin; the pin must not wait. Slice spec + post-expiry reproduction route now in PREREGISTRATION_E2 §2; PLAN §5 ask 1.
- **MeTMaP into Paper A before tomorrow's submission.** MeTMaP (arXiv:2402.14480, FORGE 2024) metamorphically audits 203 vector-matching configurations (29 encoders × 7 metrics, max accuracy 41.5%) — the nearest prior audit of the gate substrate, uncited in Paper A's final draft. One related-work sentence + one bibliography entry closes the "not the first audit" objection at the source, before the version of record exists. (Differentiation: researcher-constructed metamorphic triplets vs institution-produced labels at shipped operating points with overlap controls and transfer.)
- **Rescue the bis audit's recompute path (minutes, before any fleet reboot).** Annotator B's 200-item verdict vector was written to `/tmp/annotator_B_judgments.json` on a fleet node; annotator A's is uncommitted. If a reboot eats them, the audit's headline numbers (84.8% / 37% / κ 0.662) keep only aggregate-level provenance. Retrieve both into `results/verification/` (amendment A8).

## 2. Applied today — PREREGISTRATION_E2 sharpened pre-freeze (no numeric bar moved)

| defect (lens) | fix now in the draft |
|---|---|
| Δ(L) = Δ₀·(k/L)^α unidentifiable — only Δ₀·k^α enters the likelihood; the NevIR prediction referenced the meaningless Δ₀ (stats №2; orchestrator independently) | **k ≡ 64**, so Δ₀ ≡ Δ(64), the measured intercept; NevIR anchor restated against Δ(64) |
| L\*(0.75) undefined for configs starting < 0.75 at L=64; truncation-regime crossings extrapolated; no CI; carrier curve unstated (stats №3; framing №13) | dilution-L\* vs truncation-L\* as two named quantities; monotone spline as L\* carrier; ≥5-bin rule for parametric fits; censoring codes `≤64` / `>4096` / `>window`, never extrapolations; anchor-bootstrap CI on every L\*; gate-term critical length defined |
| L's unit ambiguous (whitespace vs per-encoder tokens) — cross-encoder L\* comparisons inherit it (data №5 arithmetic; orchestrator) | L defined as whitespace-token count (construction unit); per-encoder realized subword counts recorded and reported; special-token window arithmetic tabled pre-tag |
| Half-bin rule at L=64 drops ~half the anchors (payload p90=38 ws ≈ >32 subword cut) — composition drift confounds the intercept (data №5; stats №7) | **L=96 bin added**; **constant cohort** (eligible at every fitted bin) is the primary fit population, full set sensitivity; per-bin attrition tables pre-tag |
| Numerics pin absent — a 1–3 GPU-day two-box sharded sweep with a post-hoc parity dispute is unadjudicable; and D4's log line already **rules** E2's regime ("gte/mxbai CPU-only per the parity ruling"), which the draft never encoded (orchestrator; auditor F2; RUNBOOK parity lesson) | Ruled regime written into §2: pinned-path numerics carried from Paper A/E1; gte/mxbai CPU-only; per-config device assignments fixed (Forge: bge, e5×2, nomic-clustering, nomic-MRL-256 · Agora: MiniLM, mpnet); long-context nomic as a **new named configuration** (Forge, fp32, revision pinned at tag); GPU configs re-pass the frozen-probe parity gate extended with titrated-length probes; merge hash covers checkpoint+device+dtype+library; wall-clock note flags that the two CPU-only configs put multi-day Wu-CPU time on the critical path — re-ruling to fresh numerics stays available by amendment, as a ruling not a default |
| H2's intersection over 10 configs on raw point estimates fails by noise with better-than-even odds; "ecological" ambiguous between raw-SPLICE and NATIVE (stats №7) | primary = SPLICE raw; per-config anchor-bootstrap 95% upper bound below the bar; "by L=512" = that bin and every larger within-window bin; NATIVE = confirmatory |
| H3's truncation clause is P=1 arithmetic registered as hypothesis; its substantive clause is boundary-contaminated at exactly L=512 (stats №10) | split: **H3a** within-window (L ≤ window/2, AUROC metric — dodges H4's miss-rate ceiling Δ ≤ 0.05) vs **H3b** reported deployment fact, zero confirmatory weight |
| H5 tautological in SPLICE (byte-identical hosts force r→0 as O(1/L)) and its "mechanically explains §7" bridge is false — §7's repair failed at *sentence* scale (0.750→0.533) where dilution is absent (stats №11) | demoted to calibration: observed r(L) reported against its closed-form prediction, deviation is the finding; §7 bridge replaced with "additional, independent mechanism"; non-arithmetic version registered in E1 (amendment A4.v) |
| H6 as 140 pointwise 95% checks fails under *perfect* transfer (stats №13; auditor F10 sized the budget) | registered criterion: ≥ 126/140 cells inside the frozen curves' 95% prediction bands AND no configuration with > 3 of its 14 cells outside; every miss listed |
| miss-rate-only measurement can't distinguish blindness from degeneracy (framing №7) | both gate error directions per cell (miss + false-block); **degeneracy twin** named descriptive: by L≥256 at ≤0.85 the gate converges to constant-pass |
| L grid has no deployment provenance (framing №10) | pre-tag chunk-size survey of the Paper A §2 artifact set (LangChain/LlamaIndex/cache defaults), giving L the same opened-and-read provenance as the thresholds |
| enwiki "exact source snapshot" is an inference — July 2021 had two dumps (data №7) | 50-passage two-dump test at mining time, winner recorded |
| negation-aware encoders deferred entirely — "you test the repairs you expect to fail and withhold the one you cite as promising" (framing №8) | exploratory arm (configs 10+, non-binding, operator ruling pending): Truong et al. checkpoints if obtainable; deep analysis stays Paper C |

## 3. Proposed amendments awaiting ratification — prereg-e1 A0–A7 (one commit)

Appended below the tag line in PREREGISTRATION_E1.md, all marked PROPOSED: **A0** tag-discharge note (the frozen banner still reads "STATUS: DRAFT" — outreach attachments would misread it); **A1** = the decision brief's D2 (sign-flip table + per-class bis breakdown registered as post-hoc), recommended 2026-08-06, still unfiled before today; **A2** leakage probe (§6 below) registered with result; **A3** document-level permutation redo (§4's test violated exchangeability — the exact bug Paper A §3 corrected in itself; Cliff's δ with |δ|<0.10 equivalence becomes the operative criterion); **A4** lexical-baseline panel, cosine-given-Jaccard partial, length-decile axis, the size-matched one-token contrast (`MUST→SHOULD` vs `teh→the`), r(Jaccard,class) at g3; **A5** the open 300-pair arm upgraded to a blind **construct** rubric (annotators judge "does correct_text change what orig_text asserts within the span," never seeing the IETF type; construct-vs-institution confusion matrix; noise-corrected AUROC ceiling printed beside every decision figure; >15% disagreement promotes the adjudicated subsample to primary); **A6** gate-term 2×2 reporting incl. false-block direction and the cosine-0.60 continuity number (the 0/56 audited guard, now against ~1,425 institution-verified prose corrections); **A7** "five shipped operating points + the audited 0.60" wording.

## 4. Post-hoc computables queued (frozen data, no fleet time)

Jaccard-alone AUROC with the pinned filter (review measured ≈0.550–0.553 full / **0.568 prose-primary** on an independent parse — approximately the naive encoder result, which is the point: *the embedding does not outperform counting words*, and stratification correctly removed exactly that); the A3 permutation redo; the A4 panel; the A6 tables; H4 sensitivity excluding the 16 leaked pairs.

## 5. Drafting debts (owed when D4 unholds — none block E2)

1. **The reconciliation branch (the one that matters):** Paper A's bge/mxbai hit 0.79–0.90 on authored reversal-vs-paraphrase at matched overlap; E1's naive errata AUROC is 0.55–0.57 → 0.50–0.52 stratified. The paper must say what construct changed (maximal authored contrast vs institutional Technical/Editorial with a measured noise ceiling) *before* a reviewer says it. The pre-written headline survives via gate-terms (H1) regardless: quantify over gate policies, not encoders.
2. Operating-point provenance: "five shipped + the audited 0.60" everywhere (A7).
3. Construct honesty: IETF's own definitions judge document-level "technical content," largely as filed by submitters — Technical/Editorial is a *proxy* the A5 arm validates, with the confusion matrix and ceiling reported; never write "the institution asserted meaning-change."
4. Ecological framing: E1 + NATIVE arm carry the banner; SPLICE is the controlled instrument (the "one-two structure" sentence is the honest frame — promote it).
5. Sign-flip wording: the bis row is machine-labeled with the audit's measured correction (precision 84.8%, negative-leak 37%); the errata and authored anchors are label-clean; state the law with the bis asterisk attached — and the 37% leak is itself the practitioner finding (a lexical change-detector missing 37% of adjudicated meaning changes is the same surface-blindness, quantified).
6. H4's "repair candidates" attribution: per-encoder threshold is *new* (motivated by §5's threshold movement); §7's actual first row was an encoder swap. Do not write "Paper A's repair candidates" unqualified.
7. Citation corrections: vCache = 2502.03771 (ICLR 2026), calibration-gap = 2606.19719 (two teams); ~10× not ~100× vs Semantic Needles, "procedurally generated perturbations"; CondaQA citation count dropped/attributed (S2: 50); add MeTMaP, Length-Collapse (2410.24200), 3GPP CR line (Chen USENIX Sec'22, TSpec-LLM), PSMBench hedge, WNC-on-semantics. Full list in the design record's corrections appendix.
8. HE3-3 vs Paper A: 0.679 on PAWS for the production path vs "at chance" at sentence scale in Paper A — the per-regime heterogeneity sentence must appear where both numbers do.
9. E1 title count 3,578 (not 3,583); Held usable = 2,214.
10. Floor Lemma writeup: state the cone condition θ ≤ π/2 (the bound as written fails for wider cones; trivially satisfied on anisotropic registers, but the lemma must say so); B3's empirical check already guards it.

## 6. Attacks that dissolved under measurement (the negation key — what was tried and failed)

- **"H4's transfer is contaminated: bis revisions incorporate the very errata the calibrations were fit on"** (stats №1, fatal-if-true). Measured on the frozen corpora (orchestrator, `bis_errata_leakage_probe.py`, preserved in the session scratchpad and quoted in A2): exposure is real — 7,645 of 13,780 bis pairs sit on errata-bearing RFCs — but textual leakage is **16 pairs (0.12%) at J≥0.9; 3 of 2,019 strength transitions; 46 (0.33%) at J≥0.5**. Incapable of moving the transfer estimates. One perfect specimen for the footnote: `bis-RFC3028-RFC5228-711` ≡ erratum `eid1493`, J = 1.0. The sign-flip law comes out armored: its strongest stated attack now carries a measured sub-half-percent bound.
- **"The corpus counts/licenses/sources will have drifted or were wrong."** Every count reproduced exactly (7,991; 1,895/1,683; 122 keyword changes; 1,289 CondaQA passages 474/115/700; Apache-2.0; enwiki-20210701 present at 18.74 GB); snapshot sha256 identical to live three days later. The one substantive nuance: the 0.889/0.912 medians are real but tokenizer-flattering (whitespace tokens give 0.833/0.867; coupling strengthens to δ=−0.137 in prose-primary) — which the executed stratified-only fallback already handles; the per-tokenizer numbers go in the paper via A4.
- **"H2's certification will fail and strand the headline"** (data №1, framing №1). It *did* boundary-fail in execution — and the pre-registered fallback clause caught it: stratified-only reporting, headline strengthened. The branch existed because the prereg wrote it down. Nothing to fix; recorded here as the discipline visibly working.
- **"The 2,556-pair NevIR headroom figure is folklore."** Verified: 948/225/1,380 across splits, from CondaQA, per the SIGIR'25 reproduction.
- **"E1's compute sizing is fantasy."** Reconstructed: 216k embeddings E1 (<1 h Forge ✓); E2 ≈1–2M calls at the actual 959-anchor yield ✓.

## 7. Cross-consistency with Paper A (framing lens, 20 rows)

17 match / 3 drift, all three now handled: operating-point count (A7), "Paper A's repair candidates" attribution (§5.6), "§9 explicitly left open" → "does not touch" (corrections appendix). Named-set discipline on 0.79–0.90 held exactly ("the strongest two"). "Read but not redistributed" is verbatim §9 — which is what made E1's original release plan indefensible until it adopted the same pattern (now in PLAN §5 ask 6: TLP §3.c.iii attributed-unmodified-excerpts + regeneration scripts; `correct_text` provenance and pre-5378 RFCs flagged).

## 8. Open commitments ledger (pre-existing; review adds none it didn't close)

| commitment | source | closes via |
|---|---|---|
| PMC slice freeze | prereg-e2 tag condition | **this week** (deadline 08-24) |
| prereg-e2 tag | D4 critical path | after PMC + lexical tables (§5 sequencing) |
| E2 harness build: splice builder, `titration_sweep.py`, `merge_shards.py`, decay fits | RUNBOOK R6 (scripts do not exist yet) | before sweep |
| errata 300-pair label-noise arm | prereg-e1 §2/§4 | A5 protocol, bis-audit staffing precedent |
| E1-P3 five-class cascade | prereg-e1 §2 (deviation disclosed as A9) | run R2 as labeled post-hoc, or retire the cascade classes by amendment |
| annotator verdict vectors + `bis_true_label_coupling.py` + `e1_bis_stats.json` regeneration | A8 (findings_h4's named-but-uncomputed correction) | **retrieve from the fleet before reboot**, one script, one commit |
| `corpus/MANIFEST.json` seven `bytes: null` entries | integrity discipline §1.1 | backfill sizes (PLAN ask 3b) |
| E4 bars freeze | floor-lemma proposal §5 | `prereg-e4` tag before any embedding pass |
| D2 amendment filing | decision brief 08-06 | A1 (in the A-block commit) |
| outreach send + record; McQuistin address ⟨VERIFY⟩; repo public or PDF attachments | paper_b_outreach.md | operator send-day |
| D5 figures ruling; D4 altitude ruling | decision brief | operator, post-E2 |

## 9. Campaign-auditor addendum (fifth lens) — landed, all findings folded

Sixteen findings against the live artifacts; every fix applied or filed same-day. The load-bearing five:

- **F1 — 959 vs 960, resolved: 959 is correct and the discrepancy was a caught-then-unreported two-source divergence.** The estimator (`verify_condaqa.py`) counts any single changed slot; the builder (`build_anchors.py`) requires a 1:1 substitution. Sole divergent candidate: **train-119**, whose "paraphrase" is a whole-sentence 0:1 *insertion* — no slot position, correctly excluded by the builder. So: 75.4% as single-slot changes, **75.3% as strict same-slot substitutions**, and the same-slot rate 1.0 belongs to the 959. The prereg's "identical to the estimator by construction" was false; PLAN and the Ravichander email carried the estimator's number with the builder's noun. Fixed in all three places (prereg §1 disclosure paragraph per PLAN §1.2; PLAN rows; outreach draft corrected with an if-already-sent correction instruction — it is the "not a single erratum in our data sets" brand on the line, addressed to the dataset's author).
- **F5 — the "measured correction" findings_h4 promises did not exist; now bounded.** No frozen JSON carries the true-label bis coupling; the annotator vectors are uncommitted (B's in `/tmp` on a fleet node — §1 rescue item). The auditor computed admissible bounds from the 35 operator verdicts + frozen aggregates: true-label median-Jaccard gap **+0.080 to +0.259** (exchangeability +0.159), **aligned in 100% of admissible reconstructions** — "sign-flip law stands hard" is retroactively licensed, but the repo must contain the computation (A8: commit vectors, add `scripts/bis_true_label_coupling.py`, regenerate `e1_bis_stats.json` to carry the 0.6667/0.8696 medians it is cited for).
- **F7 — instrument deviation found: H3 was scored on a three-class partition (kw2119/number/other) where five classes were pre-registered.** The E1-P3 cascade never ran; no LLM ever labeled an errata pair. H3's FAIL is robust to the partition (kw2119 g2 medians 0.9183–0.9832, below the 0.97 bar under any subdivision of "other") — but the silent replacement of a registered instrument is exactly what amendments exist for. Filed as **A9**; resolve before drafting (run R2 as labeled post-hoc, or retire the cascade classes).
- **F3/F4 — the drop rule and the L unit were quantitatively worse than the design admitted.** Measured on the frozen anchors: the L=64 drop ranges **21.6%–61.4%** depending on unit and member-set (whitespace-vs-×1.3, orig-vs-max-member) — and under one reading the constant cohort falls to **370 < the ≥400 fallback**. Both now pinned in §2 (anchor-level, max-member, whitespace vs W(L)/2 budget; nominal-L semantics with per-encoder realized tokens recorded; L=512 boundary bin analyzed both ways; cohort floor rule). Also §6's payload stats were the passage population's — the anchors' own are p90 41/45, max 105/112, corrected.
- **F12 — the executed bis audit matches the written discipline at context level** (protocol-only prompts verified in code; key sealed through adjudication; regex labeler shares no mechanism with either seat — the D1-circularity analogy fails on the facts, a defense the paper should state). Three documentation gaps + two executed deviations now filed in `docs/annotation_protocol_bis_notes.md` (kept outside the annotator-facing protocol deliberately): per-seat provenance unevidenced; the adjudicator was label-blind but not hypothesis-blind (ties only — record the scope); the weight-sharing residual acknowledged with the cross-family κ as mitigation.

Also folded: F2 (the D4 ruling already fixed E2's numerics regime — §2 table row corrected accordingly); F9/F10 (k=64, bar-semantics splits H2a/b/c, H6 budget ≥126/140, membership lists to §6); F11 (PLAN staleness — eight rows + 48 hours of log, all updated today); F13 (ladder footnote + the in-house HE3-3 bound: production 0.5096 stratified on errata, so 0.679 on PAWS is bounded on both sides in-house); F14 number hygiene (0.989–0.997; 0.06–0.10/0.02–0.06; "max 0.524" noted for the decision brief; MANIFEST `bytes: null` backfill → PLAN ask 3b); F15 (the true remaining pre-tag list, now §5/§6 of the prereg); F16 (E4 must declare its numerics regime — PLAN row updated).

**Auditor's survived list (verbatim-grade):** every load-bearing published number recomputed exactly (H1 89.3/89.7; H2 marginals 0.5525–0.5693 with three configs honestly boundary-failed at 0.046–0.050 movement; stratified 0.5024–0.5237; the 89–98% approval range; all H4 transfer values; E3's 28 cells, QQP 0.8541–0.8971, PAWS 0.6068–0.6790); tag integrity clean (nothing above any tag line ever edited); E3's discipline arc (freeze → pre-committed analysis → bug found → voided → amended → rerun) is the program working as designed; scorecard honesty throughout — failed bars reported as failures everywhere, no post-hoc softening found anywhere in the campaign.
