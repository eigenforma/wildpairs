# Execution notes — bis audit protocol (2026-08-08, campaign-auditor lens)

Kept OUTSIDE `annotation_protocol_bis.md` deliberately: that file is the only document an annotator may see, and these notes carry provenance an annotator must not.

**What the record shows matching the discipline (§1.5):** annotator prompts contained the protocol only (verified in `harness/e1/annotator_b_120b.py`: system = protocol text, user = the two texts, temp 0); the blind packet holds texts only; the machine-label key stayed sealed until all 35 adjudication verdicts were recorded (commit `cca7fd5`); seats A and B are independent contexts across two model families. **The direct D1-circularity analogy fails on the facts:** the bis labeler is a keyword-multiset regex, so neither audit seat shares any mechanism with the labeler — and since the E1-P3 cascade never ran (amendment A9), no LLM ever labeled an errata or bis pair in this program. State this defense in the paper; today it is only inferable.

**Gaps to record (not retro-fix):**
1. **Annotator provenance is asserted, not evidenced.** Who/what seat A was, its firewall mechanics, and what its context contained live in one provenance string in `bis_audit_final.json`. The load-bearing §1.5 boundary ("must not have seen this repo's corpora or predictions") needs a dated provenance paragraph per seat — write it now from memory while the sessions are reconstructible.
2. **The adjudicator's blinding scope was narrower than the annotators'.** The operator was blind to machine labels but not to hypotheses (the operator ruled D1–D5 and read every findings doc, then decided all 35 contested labels — 31 of 35 → "different"). Defensible: ties only, label-blind, κ agreements untouched. But D1's own sentence ("annotator and reviewer roles are mutually exclusive per person until annotation closes") is strained by the tie-breaking seat; record the scope rather than leaving it to be discovered.
3. **Weight-sharing residual.** Seat A shares weights with the agent that designed the mining rule, the hypotheses, and the audit. The written discipline is a *context* firewall and it was honored; weight-independence is not claimed anywhere. One acknowledging sentence, citing cross-family κ = 0.662 + operator adjudication as the mitigation.
4. **Executed deviations, now filed:** protocol rule 4 ("do not consult any tool, model…") is human-directed while seat B *is* a model; and seat B's driver stripped the Output-format section (harmony-template brace bug) and answered one word per item — both previously disclosed only in a code comment.

**For the errata 300-pair arm (A5):** reuse this protocol's rubric shape but judge the construct against the *span* ("does correct_text change what orig_text asserts, within the compared span?"), keep the IETF type invisible, and write the per-seat provenance paragraph on day one, not after.
