# PREREGISTRATION — E2: The Dilution Law

> **STATUS: FROZEN at tag `prereg-e2`** (2026-08-11, operator ratification). Everything above the
> amendment divider binds; amendments append below it, dated. No encoder scored any titrated
> passage before the tag. Provenance: drafted 2026-08-05, sharpened 2026-08-08 by the five-lens
> adversarial review (`docs/reviews/adversarial_review_2026-08-08.md`), pinned 2026-08-11 by the
> build and verification passes (member arithmetic, position slots, shard axes, bar semantics).
> The PMC slice was frozen two-site on 2026-08-10, ahead of the source path's 2026-08-24 deletion.

## 1. Population and payloads

- Anchors: `corpus/e2_dilution/anchors_frozen.jsonl` — **959 anchor sets** (of 1,273 candidates carrying original + paraphrase + affirmative edits) where BOTH the paraphrase (edit 1) and affirmative/reversal (edit 3) are single-sentence *substitutions* against the original passage, per the pinned diff algorithm (`harness/e2/build_anchors.py`; **stricter than** the estimator in `scripts/verify_condaqa.py` by one predicate — the estimator counts any single changed slot including insertions/deletions, the builder requires a 1:1 substitution. The two counts are 960 vs 959; the sole divergent candidate is **train-119**, whose paraphrase edit is a whole-sentence 0:1 *insertion* — no original counterpart, no slot position, cannot satisfy the byte-identical-host/identical-position requirement below — correctly excluded. Two-source check reported here per PLAN §1.2; yields 75.4% as single-slot changes, **75.3% as strict same-slot substitutions**, and the same-slot rate 1.0 is a property of the 959). 903 also carry the scope edit as the high-overlap flip control. Splits 363/86/510 (train/dev/test); split membership is recorded but pooled for the titration (CondaQA's QA splits are irrelevant to pairwise gate decisions; no model is trained).
- Pair classes per anchor: **flip** = (payload_orig → payload_affirm), **faithful** = (payload_orig → payload_para), **scope** (secondary) = (payload_orig → payload_scope).
- Every anchor's paraphrase and affirmative edits modify the SAME sentence slot (measured rate 1.0), so both members of every pair share byte-identical host context with the payload at an identical position.

## 2. Design

- **Length axis, defined.** L is the **nominal bin label**, a construction unit: each titrated passage is assembled once to W(L) = ⌈L/1.3⌉ whitespace tokens and shared **byte-identically across all encoders** (per-encoder bins would destroy the byte-identity of the stimulus set and multiply the corpus tenfold). **Member arithmetic, pinned (build 2026-08-11):** the host frame is sized to W(L) − ws(payload_orig), so the *orig* member realizes exactly W(L); the other member differs only by the payload's own length delta — byte-identical hosts force this, and both members' realized lengths are recorded. **Position slots, pinned:** slot = ⌊q·(m+1)⌋ for q ∈ {0.05, 0.50, 0.95} over the host's m sentence slots; at small L the three positions can collide on one slot — collisions are counted per bin and disclosed, never smoothed. Bins L ∈ {64, 96, 128, 256, 512, 1024, 2048, 4096} (the 96 bin added 2026-08-08 against the intercept-bin attrition cliff). The per-(encoder, passage) **realized token count under the encoder's own tokenizer is recorded**, its per-bin median reported as a secondary axis; all L\* values are stated on the nominal axis with realized-token medians alongside. **Truncation-regime membership is decided per (encoder, passage) by realized tokens > effective window** (window arithmetic **includes special tokens**, computed and tabled before the tag); at the boundary bin L=512, the fraction of passages exceeding each 512-window is reported and that bin is analyzed both including and excluding them.
- **SPLICE arm (primary instrument).** The payload sentence is embedded in real host text at controlled position ∈ {early, middle, late} (first/middle/last decile of sentence slots), titrated across the L grid. **Drop rule, pinned:** an **anchor** drops from bin L when max(whitespace tokens of payload_orig, payload_para, payload_affirm) > W(L)/2, where W(L) = ⌈L/1.3⌉ is the bin's whitespace construction budget — anchor-level by definition, so flip and faithful cells always contain identical anchor sets. Measured attrition on the frozen anchors (max-member): the unit and member-set choices move the L=64 drop between 21.6% and 61.4%, which is why they are pinned here and not left to the harness; per-bin n and the dropped anchors' payload-length distribution are reported per cell. **Cohorts:** the *constant cohort* (anchors eligible at every fitted bin) is **co-primary** for all decay fits and cross-L comparisons alongside the full per-bin population; a qualitative disagreement between the two curve families is itself a reported finding. The cohort n under the pinned rule is stated in §6 before the tag; if it lands below 400, the L=96 bin (not L=64) becomes the fit floor for the constant cohort and the fact is disclosed.
- **Host assignment (pinned before tag).** Which enwiki articles qualify as hosts, how many host draws per (anchor, L, position), the RNG seed, and the contamination exclusion — a host must not contain the payload sentence or the anchor's source passage — are specified in §6 at tag time; hosts are drawn once and shared byte-identically across all encoders. Hosts: **enwiki-20210701** (fit domain; frozen, sha256 `cf5ab6b3…`), **PMC-OA CC-BY slice** (held-out 1; **pin and freeze NOW from `s3://pmc-oa-opendata/deprecated/oa_comm/txt/all/` — the deprecated prefix is deleted on or after 2026-08-24** (NCBI announcement 2026-02-12); slice = ascending PMC ID over the AWS OA `oa_comm` txt listing — the listing itself snapshotted into MANIFEST at retrieval — body ≥ 6,000 **whitespace** tokens, first 2,000 qualifying articles, no other exclusions; license file lists frozen with sha256s; post-expiry reproduction route recorded as the eSearch→S3 per-article pipeline), **RFC text corpus** (held-out 2; frozen shared snapshot with E1; spliced RFC derivatives are never redistributed — regeneration script + checksums only).
- **NATIVE arm (supporting, ecological).** The CondaQA passage extended with its actual surrounding article text from the pinned dump; failures drop from NATIVE only. **Measured 2026-08-11:** match rate **94.8%** (820 full / 402 partial / 67 none of 1,289; `results/verification/e2_native_scan_summary.json`), above the hedged 70–90%. **Two-dump test resolved:** a 50-passage sample sits byte-identically (normalized) in both July 2021 dumps at 44/50 with zero discriminating cases (6 in neither; `e2_two_dump_test.log`) — the source-dump distinction is immaterial for the matched set; the frozen **20210701** is the NATIVE host source, disclosed as such.
- **Host assignment, pinned (2026-08-11, ratified at tag).** Host pool per domain: enwiki = ns-0 non-redirect articles with ≥ 3,151 ws tokens (W(4096)) from the scan's `articles_index.tsv`, **excluding every article matched to a CondaQA passage** (titles known from the scan) and any host containing the payload or source passage (the build's contamination exclusion); PMC = the frozen 2,000-article slice (qualifies by construction); RFC = frozen rfc-text documents under the same length rule. **One host document per (anchor × domain), drawn once with seed 20260805, reused across all (L, position) cells** — dilution is titrated within a fixed host, so cross-L comparisons never confound host identity; each (L, position) frame is carved deterministically from that document around the payload slot.
- **Encoders, numerics, and device pins (fixed pre-sweep).** The nine pinned polaritycheck configurations + long-context nomic at its full window (the largest titrated bin is 4096; window headroom beyond that is reported as unused rather than promised). **E2 scores on the pinned polaritycheck reference numerics carried from Paper A/E1 — not E3's fresh-fp32 regime — per the D4 ruling line ("gte/mxbai CPU-only per the parity ruling").** gte-base and mxbai-embed-large are fp16-native and **CPU-only on the pinned Wu-CPU path**; the remaining seven pinned configs may run on GPU, **one pinned device per config, fixed here: Forge (3090): bge, e5 [query:], e5 [no prefix], nomic-clustering, nomic-MRL-256 · Agora (16 GB): MiniLM, mpnet.** Long-context nomic is a **new named configuration** outside the Paper A registry: Forge, fp32, checkpoint revision pinned at tag time. Before any shard is admitted, each GPU-assigned config re-passes the frozen-probe parity gate (cosine delta ≤ 1e-5 vs the pinned path; probe set extends `results/verification/h4_gpu_parity_probes.json` with titrated-length probes at L ∈ {64, 512, 4096}). Shards merge only through `merge_shards.py --verify-config-hash`, where the hash covers checkpoint hash + device + dtype + library version; a shard bearing an unpinned triple is rejected. A device failure re-pins by dated amendment below the tag (E3 amendment A1 precedent). **Shard axes, pinned (build 2026-08-11):** anchors shard by id-hash parity (RUNBOOK R6) *and* each node sweeps only its pinned config slice (§2 assignments) — two orthogonal axes, both enforced; R6's flag set predates the build/sweep split and gets its runbook amendment at tag. gte/mxbai run **checkpoint-native, no dtype cast** (forcing fp32 would alter the pinned Wu-CPU numerics — the parity lesson); observed parameter dtype is recorded in shard provenance. *Wall-clock note for the operator: the two CPU-only configs put multi-day Wu-CPU time on the critical path at this grid; if that binds, the E3 precedent (a fresh-numerics pin for a fresh experiment) is available by ruling — but it is a ruling, not a default.*
- **Measurements** per (encoder, L, position, host, arm): flip-vs-faithful decision AUROC over anchors; cosine-gap Δ distributions; **both gate error directions** at operating points {0.30, 0.40, 0.60, 0.80, 0.85, 0.95} — flip *miss* rate AND faithful *false-block* rate (a gate that converges to constant-pass shows miss→1 with false-block→0; reporting one direction cannot distinguish blindness from degeneracy). Operating-point provenance: the five shipped points of Paper A §5 plus the audited guard's cosine-0.60 equivalent (Paper A §6).
- **Fits.** **Δ(L) = Δ₀·(64/L)^α** per encoder on Wikipedia hosts — k is pinned to 64 so Δ₀ ≡ Δ(64), the measured intercept (the three-parameter form Δ₀·(k/L)^α is unidentifiable: only Δ₀·k^α enters the likelihood). A monotone-spline nonparametric curve is fit alongside; **the spline is the L\* carrier** (the power law is the parametric summary). Fit domain: within-window bins only (truncation bins excluded, listed per encoder); a parametric fit is attempted only where ≥ 5 within-window bins exist, else spline-only. **L\* definitions:** dilution-L\*(A) = first within-window crossing of AUROC = A on the fitted spline (A ∈ {0.75, 0.55}); truncation-L\* = the window edge, reported as a separate named quantity, never pooled with dilution-L\*. Censoring codes are reported, never extrapolated values: "≤ 64" (already at or below the level at the first bin), "> 4096" (no crossing in domain), "> window" (crossing would sit in the truncation regime). Every reported L\* carries an anchor-level bootstrap CI (refit per resample). Gate-term critical length (named descriptive): smallest L at which flip miss rate ≥ 0.99 at every operating point ≤ 0.85.

## 3. Hypotheses and bars

Primary endpoints are the H-numbered bars below, each with its aggregation stated; the full measurement grid is descriptive and carries no pass/fail. All CIs and tests bootstrap over anchors (the repeated unit), never over cells.

- **Scoring convention for every bar:** "below X **by** L=B" means the quantity is ≤ X at bin B **and at every larger within-window bin**; monotonicity violations are reported per cell, never smoothed. The **membership lists** — which of the nine count as mean-pooled (H1), and which configurations the NevIR at/below-random set names (external anchor) — are enumerated in §6 at tag time, not left implicit.
- **H1 (calibration, not discovery):** α ∈ [0.8, 1.2] for the mean-pooled set — the near-arithmetic dilution of a one-sentence perturbation; deviations by CLS-pooling/attention encoders are reported as findings. **Sharp form:** the arithmetic lexical decay Δ_lex(L) (token-Jaccard gap under the pinned tokenizer) is overlaid and α_encoder is tested against α_lex = 1; an encoder whose α departs from its own lexical arithmetic is the finding.
- **H2 (the headline — universal failure length), three separately scored clauses** (bundled, one clause's failure would poison the others' clean passes):
  - **H2a:** every configuration, including the bge/mxbai class (AUROC 0.79–0.90 at sentence scale in Paper A — the named-set figure, strongest two), falls below flip-vs-faithful AUROC 0.60 by L=512 on the **SPLICE arm, raw** (primary).
  - **H2b:** below 0.65 by L=1024, caliper-matched.
  - **H2c:** the production nomic-MRL-256 path ≤ 0.55 **at every bin including L=64** (95% CI containing or below 0.55 in every within-window SPLICE cell). *Scoring note: the lab's own E3 result (production path 0.679 on PAWS at sentence scale) marks the short-L end of this clause as a real risk; the NevIR anchor is the stated defense; a short-L failure here is reported as such, not absorbed.*
  - **Intersection test for H2a/H2b:** per-config anchor-bootstrap 95% upper bound below the bar (upper bound = the 97.5th bootstrap percentile, the two-sided 95% CI's upper end) — never raw point estimates (at per-cell n, point-estimate intersections fail by noise with better-than-even odds across ten configs). **Evaluability (verification 2026-08-11):** each config is scored on its *within-window* bins at the bar's L and above; for 512-window configs at L=512 that is the boundary bin's primary variant (realized-over-window passages excluded, per the dual analysis). A config with zero evaluable cells for a clause is named **vacuous** for that clause, excluded from the intersection, and counted beside the verdict — vacuous configs are never counted as passes. The NATIVE arm is confirmatory-ecological for the same bars.
- **H3a (position, within window):** at L ≤ window/2 per encoder (so boundary clipping cannot contaminate the cell), AUROC(early) − AUROC(late) ≥ 0.05 for ≥ half the configurations — metric pinned to AUROC (threshold-free; miss-rate deltas collide with H4's ceiling: pooled miss ≥ 0.95 caps any position delta at 0.05).
- **H3b (truncation, reported deployment fact — not a hypothesis):** late-position flips beyond a 512-window encoder's effective window are missed at rate 1.0 by construction; reported as deployment behavior with per-encoder effective-window arithmetic, carrying zero confirmatory weight.
- **H4 (shipped thresholds):** on the SPLICE arm, positions pooled: at cosine 0.85, flip miss rate ≥ 0.95 for every configuration at L ≥ 256; at 0.95, the contradicting passage is judged redundant — i.e., misses at the 0.95 operating point — ≥ 90% at L ≥ 128. **Degeneracy twin (named descriptive):** by L ≥ 256 at thresholds ≤ 0.85, the gate's two error rates converge to (miss → 1, false-block → 0) — the gate is not misjudging pairs, it has become a constant.
- **H5 (confounder starvation — calibration, not discovery):** |r(whole-passage token-Jaccard, class)| < 0.10 by L=512, r = point-biserial Pearson of whole-passage token-Jaccard against the binary class, same "by" semantics as H2. **Scale note (verification 2026-08-11):** the raw whole-passage Jaccard *gap* decays as O(1/L) by construction (byte-identical hosts confine class information to the payload) — but r is scale-free, and separation and spread shrink together, so r(L) need not decay; the bar may bind at every L or none. The registered reference is therefore the **closed-form r(L)** computed from the payload-Jaccard distributions under the splice arithmetic; the observed curve is reported against it and **the deviation, not the decay, is the finding** — whichever way the bar resolves. This is an additional, independent reason overlap-conditioned repair is information-starved at deployment lengths — **distinct from** Paper A §7's cross-author instability, which was measured at sentence scale where dilution is absent (0.750 → 0.533 held-out). The non-arithmetic version of this quantity lives in E1 (r at section-context granularity on natural errata) and is registered there as a post-hoc amendment.
- **H6 (transfer):** Wikipedia-fitted decay curves predict PMC and RFC AUROC. **Registered criterion, stated structurally (the grid is 8 bins since the 96 addition, and truncation cells are not evaluable): over the n evaluable held-out cells (within-window (config, bin, host) cells across the two held-out hosts), PASS = ≥ ⌈0.9·n⌉ inside the frozen curves' 95% prediction bands AND no single configuration with > 3 of its evaluable cells outside; n and every miss are listed.** (Pointwise per-cell scoring guarantees ~5% chance failures under perfect transfer; that failure mode is excluded by construction.)
- **External anchor (NevIR):** the configurations NevIR's published results score at/below random on passage-scale negation ranking (set enumerated in §6 at tag time, with citation) show **Δ(64) < 0.05** — the intercept is externally anchored at the measured first bin (not at a fit parameter); NevIR's pairwise-accuracy metric is reported on the L=64 cell as a bridge baseline.
- **Exploratory arm (non-binding, operator ruling pending):** publicly released negation-aware encoder checkpoints (Truong et al. 2025 at minimum, if obtainable) run through the same grid as configurations 10+, labeled exploratory, outside every bar above — closing the "you never test the repair you cite as promising" review before it is written. Their deep evaluation (survival of reported gains under overlap matching) stays deferred to Paper C.

## 4. Analysis

AUROC with anchor-bootstrap CIs (10,000, seed 20260805); ecological (raw) AND caliper-matched (payload-Jaccard within ±0.05) analyses both reported per Paper A §3 discipline; scope-edit class as high-overlap flip control; per-length-bin r(Jaccard, class) vs its arithmetic prediction (H5); Δ_lex(L) overlay (H1); both gate error directions at all six operating points (H4 + degeneracy twin); κ from the E2-P5 two-annotator relabel (200 anchors, standalone semantics, **annotators blind to all hypotheses**) reported as a label-noise ceiling beside every decision-axis figure; all results frozen JSON, one command, offline.

**Deployment provenance of the L grid (encoder-blind, pre-tag):** a half-day survey of default chunk/window sizes in the Paper A §2 artifact set (LangChain splitter defaults, LlamaIndex node sizes, semantic-cache entry lengths) is recorded in §6 before the tag, giving the L range the same opened-and-read provenance Paper A gave its thresholds. The titration then measures the failure at the lengths the gates actually compare, by their own defaults.

## 5. Sequencing

**PMC slice frozen from the deprecated S3 path (deadline 2026-08-24)** + enwiki dump-version check → per-encoder tokenizer/attrition tables + chunk-size survey (all lexical) → `prereg-e2` tag → titration build (encoder-blind) → relabel arm in parallel → embedding sweep (Forge/Agora shards under the §2 numerics pin, config-hash-verified merge) → fits on Wikipedia → frozen prediction on PMC/RFC. Amendments after the tag file below the tag line.

## 6. Computed before this draft (encoder-blind, lexical, disclosed)

From `harness/e2/build_anchors.py` + `scripts/verify_condaqa.py` on the frozen snapshot (all counts two-source checked where a second source exists): 14,182 rows, 1,289 passages, 474/115/700, zero cross-split PassageID overlap; single-sentence rates paraphrase 0.909 / scope 0.790 / affirmative 0.764; **959 anchors frozen, 903 with scope control, same-slot rate 1.0**; payload token-Jaccard medians **paraphrase 0.7692 vs affirmative 0.7667 (median gap −0.0025** — the imported CondaQA confounder is negligible at the median; means 0.6759 vs 0.7147, gap 0.039, so the caliper-matched secondary analysis stays mandatory**)**; payload length, **anchor population** (the passage population's p90 39 appeared here before review): payload_orig median 23 / p90 41 / max 105 whitespace tokens; max over the three payload members median 27 / p90 45 / max 112 — the drop rule and attrition tables are computed on these ("average ~38+" in the 2026-08-05 design doc was a population p90, corrected in review).

*Landed 2026-08-10 (recompute: `scripts/e2_attrition_tables.py`; frozen: `results/verification/e2_attrition_tables.json`): the pinned-rule attrition table — L=64 drops 547/959 (57.0%), L=96 drops 20.1%, L=128 7.2%, ≥256 ≈ 0; **constant cohort over all bins = 412 ≥ 400, so L=64 stays the constant-cohort fit floor** (96-floor cohort 766 reported as sensitivity); scope-control eligibility per bin included; anchor payload stats above reproduced exactly. PMC slice freeze from the deprecated path launched on Agora coldstore same day (`scripts/pmc_freeze.py`; listing snapshot + numeric-ascending walk + filelists + sha256s).*

*Landed 2026-08-10, second batch ([docs/e2_pretag_provenance.md](docs/e2_pretag_provenance.md) carries full evidence): **H1 mean-pooled membership = 6 of 9** (e5 ×2, gte, nomic-clustering, MiniLM, mpnet; CLS: bge, mxbai, nomic-MRL-256-production — pooling is a configuration property, the same nomic weights sit in both sets); **NevIR external anchor: all-mpnet-base-v2 is the sole directly tested member of the nine** (8.1% vs 25% random, Table 2) — model-level clause applies to it alone, class-level otherwise; **chunk-size survey**: deployed defaults concentrate on the 1024 bin (LlamaIndex 1024 tok, kernel-memory 1000 tok, LangChain ≈800 ws-tok; GPTCache ships no length default), none below 512 — the L grid now carries opened-and-read deployment provenance. **PMC slice frozen two-site** (agora coldstore + forge vault, MANIFEST entries, deadline beaten).*

*Landed 2026-08-11, final batch — **the tag-time list is complete**: NATIVE match rate 94.8% and the two-dump resolution (§2); host assignment pinned with seed (§2); tokenizer conversion + effective-window tables (`scripts/tokenizer_conversion_tables.py` → `results/verification/e2_tokenizer_tables.json`): ×1.3 covers the fit domain (Wikipedia medians 1.26–1.31) while the RFC register runs 1.36–1.56 median / up to 2.13 p90 — realized-token medians per cell (the pinned second axis) carry the overrun, and p90 stimuli at nominal L=512 truncate on the seven 510-effective-window configs, which the registered L=512 dual analysis absorbs; specials = 2 everywhere; nomic windows 8190 (at L=4096 on RFC, p90 realized can brush even that — flagged). **The tag now waits only on operator ratification** (this document as amended + the two "ratified at tag" pins + the harness verification pass).*

---

## Amendments (append-only, below the `prereg-e2` tag line)

- **A1 — numerics re-pin for gte-base and mxbai-embed-large (2026-08-11, operator ruling; E3
  amendment-A1 precedent).** §2 pinned both fp16-native checkpoints to the Paper A/E1 Wu-CPU
  reference path, with the wall-clock caveat the same section recorded. Measured on the first
  two executed cells (gte, Wu CPU: 0.460 s/pair at L=64, 0.649 s/pair at L=96), the pinned path
  projects to **≈512 CPU-hours (21 days)** for these two configurations over the built grid
  (218,415 pairs, 510-token cap) — the caveat bound. **Ruling: for E2 only, gte-base and
  mxbai-embed-large run under declared fresh fp32 GPU numerics (Agora), exactly as E3 declared
  its own regime for a fresh experiment.** The other seven configurations keep the pinned-path
  regime and must still clear the §2 parity gate before any shard of theirs is admitted.
  **The seam is measured, not assumed:** the parity gate (`scripts/e2_parity_gate.py`) scores
  these two on both paths at L ∈ {64, 512, 4096} and reports max |Δcos| per bin; that number is
  published beside every gte/mxbai figure and beside the L=64 bridge to Paper A. No E2 bar
  compares these two configurations' cosines to any Paper A or E1 cosine; all E2 bars are
  within-E2 comparisons (decay, L\*, thresholds), which the regime change leaves intact.
  Everything above the tag line is unchanged.
